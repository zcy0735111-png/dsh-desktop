# DSH Desktop v0.1.0

把 **DeepSeek Harness** 的 Web GUI 封装成 Windows 原生桌面应用。

## 下载

- **`DSH Desktop Setup 0.1.0.exe`**（本页下方 Assets，约 154MB）：安装包，双击安装即用
- 系统要求：**x64 Windows 10/11**，**零依赖**（应用内置 DSH 运行时与 node.exe，无需安装 Node.js）

> ⚠️ 首次运行会有 Windows SmartScreen 提示（应用未购买商业代码签名证书）：点「更多信息 → 仍要运行」即可。

## 特性

- **独立窗口**：不再占用浏览器标签页，原生窗口运行完整 GUI
- **自动托管服务器**：启动时若检测到已有 DSH 服务（3080 起）就直接挂接；否则自动拉起 `dsh web`（隐藏窗口、日志落盘）
- **退出即清理**：只终止自己拉起的服务器进程树，不会误杀你手动启动的实例；服务器意外崩溃会提示并支持一键重启
- **零依赖分发**：内置 DSH 运行时 + node.exe，转发给别人无需任何环境准备
- **第三方插件兜底**：若你的 `~/.dsh/profiles/web` 引用了内置运行时没有的插件，会依次尝试：内置运行时 → 本机其他含该插件的 DSH 安装 → 弹窗询问是否「忽略该插件并启动」（先备份配置再移除）
- **复用现有配置**：沿用 `DSH_HOME`（默认 `~/.dsh`），会话、凭据、设置全部延续

## 安装后第一步

应用不包含任何个人信息（会话与 API 密钥都在各自电脑本地）。首次使用请在 GUI「设置」里配置你自己的模型服务商凭据。

## 从源码构建

```powershell
npm install
npm run dist          # 免安装版：dist/win-unpacked/DSH Desktop.exe
npm run dist:installer  # NSIS 安装包
```

详见 [README](https://github.com/zcy0735111-png/dsh-desktop#readme)。
